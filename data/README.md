# Data

This folder is reserved for datasets and derived artifacts.

- `raw_knot/` remains at the repository root for historical compatibility.
- New datasets can be placed under `data/raw/` and `data/processed/`.
